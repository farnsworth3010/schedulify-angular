export interface Schedule {
  audience: string;
  course: string;
  day_number: string;
  faculty: string;
  group_id: string;
  group_name: string;
  id: string;
  lesson_number: string;
  subgroup_id: string | null;
  subject: string;
  teacher: string;
  upd: string;
}
